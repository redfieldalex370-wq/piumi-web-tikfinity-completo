import AdminShell from "@/components/admin/AdminShell";
import { getSupabaseServerClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function AdminDashboard(){let stats={commissions:0,active:0,artworks:0,prices:0,likes:0,gifts:0,bridge:"offline"};try{const supabase=getSupabaseServerClient();const[{count:commissions},{count:active},{count:artworks},{count:prices},{data:live},{data:bridge}]=await Promise.all([supabase.from("commissions").select("*",{count:"exact",head:true}),supabase.from("commissions").select("*",{count:"exact",head:true}).not("status","in",'("entregado","cancelado")'),supabase.from("artworks").select("*",{count:"exact",head:true}),supabase.from("price_settings").select("*",{count:"exact",head:true}),supabase.from("live_daily_user_stats").select("likes,gifts"),supabase.from("live_bridge_status").select("status,last_seen_at").eq("singleton_key","default").maybeSingle()]);stats={commissions:commissions||0,active:active||0,artworks:artworks||0,prices:prices||0,likes:(live||[]).reduce((a,r)=>a+Number(r.likes||0),0),gifts:(live||[]).reduce((a,r)=>a+Number(r.gifts||0),0),bridge:bridge?.status||"offline"}}catch{}
return <AdminShell title="Resumen" description="Todo el universo de Piumi desde una sola cabina."><div className="dashboard-grid"><Card label="Comisiones totales" value={stats.commissions} href="/admin/comisiones"/><Card label="Comisiones activas" value={stats.active} href="/admin/comisiones"/><Card label="Dibujos publicados" value={stats.artworks} href="/admin/galeria"/><Card label="Servicios y precios" value={stats.prices} href="/admin/precios"/><Card label="Likes históricos" value={stats.likes} href="/admin/stream"/><Card label="Regalos históricos" value={stats.gifts} href="/admin/stream"/></div><section className="admin-card dashboard-welcome"><div><p className="eyebrow">ESTADO DE TIKFINITY</p><h2>{stats.bridge==="online"?"Puente conectado":"Puente desconectado"}</h2><p>El puente local es quien transporta los eventos desde TikFinity hasta Supabase.</p></div><a className="btn-primary" href="/admin/stream">Abrir módulo Stream</a></section></AdminShell>}
function Card({label,value,href}:{label:string;value:number;href:string}){return <a href={href} className="dashboard-card"><p>{label}</p><strong>{Number(value).toLocaleString("es-MX")}</strong><span>Abrir →</span></a>}
